# ======================================
# BACKEND ENVIRONMENT VARIABLES
# ======================================
# For local development, copy this file to .env
# For Render/Vercel deployment, set these in the platform dashboard

# ======================
# Database Configuration
# ======================
# MongoDB Atlas connection string (REQUIRED)
# Get from: https://cloud.mongodb.com
# Format: mongodb+srv://<username>:<password>@<cluster>.mongodb.net/<database>
MONGO_URL=mongodb+srv://username:password@cluster.mongodb.net/trading_platform

# ======================
# Server Configuration
# ======================
# Port (Render/Vercel will set this automatically, keep 3002 for local dev)
PORT=3002

# ======================
# Authentication (CRITICAL)
# ======================
# JWT Secret - MUST be strong random string (minimum 64 characters)
# Generate using: node -e "console.log(require('crypto').randomBytes(64).toString('hex'))"
JWT_SECRET=REPLACE_WITH_STRONG_RANDOM_SECRET_MINIMUM_64_CHARACTERS

# ======================
# CORS - Frontend URLs (REQUIRED for production)
# ======================
# Frontend landing page URL
# Local: http://localhost:3000
# Production: https://yourdomain.com (your deployed frontend URL)
FRONTEND_URL=http://localhost:3000

# Dashboard application URL
# Local: http://localhost:3001
# Production: https://dashboard.yourdomain.com (your deployed dashboard URL)
DASHBOARD_URL=http://localhost:3001

# ======================
# Optional Configuration
# ======================
# Stock price update interval in milliseconds (default: 5000ms for development)
PRICE_UPDATE_INTERVAL_MS=5000

# ======================================
# DEPLOYMENT INSTRUCTIONS
# ======================================
# 
# For Render (Backend):
# 1. Connect your GitHub repository
# 2. Set Build Command: npm install
# 3. Set Start Command: npm start
# 4. Add all environment variables above in Render dashboard
# 5. After frontend/dashboard deployed, update FRONTEND_URL and DASHBOARD_URL
#
# For Vercel (Backend - Functions):
# 1. Vercel is recommended for frontend/dashboard, not backend
# 2. Use Render, Railway, or Heroku for backend
# 3. Set all environment variables in platform dashboard
#
# IMPORTANT:
# - Generate a strong JWT_SECRET for production
# - Update FRONTEND_URL and DASHBOARD_URL after deploying frontend/dashboard
# - Ensure MongoDB Atlas allows connections from your hosting platform IP
#   (Set to 0.0.0.0/0 for cloud platforms)

